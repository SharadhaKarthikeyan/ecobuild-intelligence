# OpenAI API Configuration (Optional)
# If provided, the AI Assistant and summary generator will use GPT-4/GPT-3.5.
# If omitted or left empty, the application falls back to robust rule-based logic.
OPENAI_API_KEY=your_openai_api_key_here
